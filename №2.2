#include <iostream>
#include <string>
using namespace std;
int main() {
    int a,b,a1;
    cout <<"What nunber to reduce to what? : ";
    cin >> a >> b ;
    while (a > b){
        if ((a/2)>=b){
            if (a%2==0){
                a1=a;
                a=a/2;
                cout <<a1<< "/2 = " << a <<"\n";
            }
            if (a%2!=0){
                a1=a;
                a=a-1;
                cout <<a1<< "-1 = "<< a<< "\n";
            }
        }
        if ((a-1)==b){
            a1=a;
            a=a-1;
            cout <<a1<< "-1 = "<< a << "\n";
        }
    }
    if (a == b){
        cout << a << " = "<< b <<"\n"; 
    }
}
